language: python
python:
  - "3.4"
install:
    - pip install -r requirements.txt
    - pip install braintree coveralls mock mysqlclient
env:
  global:
    - TWILIO_ACCOUNT_SID="blah"
    - TWILIO_AUTH_TOKEN="blah"
    - TWILIO_NUMBER="blah"
    - TELEGRAM_BOT_TOKEN="blah"
  matrix:
    - DB=sqlite
    - DB=mysql
    - DB=postgres
addons:
  postgresql: "9.4"
script:
  - coverage run --omit=*/tests/* --source=hc manage.py test
after_success: coveralls
cache: pip
notifications:
    slack: andela:vhzCzytpng8f35nG3Yca5Ag3
